package basics;

public class Constants {
//Constants are
    public static void main(String[] args) {

        int accountBalance = 100;
        accountBalance = accountBalance + 200;
        System.out.println(accountBalance);

        final int routingNumber = 1212344;
        System.out.println(routingNumber);

        int speedLimit = 65;
        speedLimit = speedLimit + 10;
        System.out.println(speedLimit);








        //regular variable is the one that changes with time (accountBalance)
        //constant variable is the one that does never change - starts with final int(routingNumber)


    }
}
